// Task-3 stub: a minimal placeholder mount so app/page.tsx has a stable hero
// slot for the single-scroll shell. The real hero (stripes, headline, hero
// stats) is built in Task 4.
export default function Hero() {
  return <section className="hero" />;
}
